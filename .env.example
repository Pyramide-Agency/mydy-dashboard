# ── App ────────────────────────────────────────────────────────────────────────
# Generate with: docker compose exec app php artisan key:generate --show
APP_KEY=base64:CHANGE_ME

# Public URL of the app (used as APP_URL in Laravel + CORS origin)
APP_URL=https://yourdomain.com

# Port to bind on the host (ignored if Dokploy/Traefik handles routing)
APP_PORT=80

# ── Database ───────────────────────────────────────────────────────────────────
DB_DATABASE=personal_dashboard
DB_USERNAME=dashboard
DB_PASSWORD=CHANGE_ME

# ── AI (optional) ──────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-CHANGE_ME
